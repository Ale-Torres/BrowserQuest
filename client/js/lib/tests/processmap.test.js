/*JS Unit testing using Jest*/
const processmap = require('./processmap').processmap;

/*test function input output */
describe('processMap', () => {
   test('client_name', () => {
      //inputs
      const Info = '';
      const description = '';
      const jestinformation = '';

      //test
      const result = extractInfo(Info);
    
      //expect
      expect(result).toEqual(expected);
   });
  
     test('length', () => {
      //inputs
      const Info = '';
      const description = '';
      const jestinformation = '';

      //test
      const result = extractInfo(Info);
    
      //expect
      expect(result).toEqual(expected);
   });
  
     test('delay', () => {
      //inputs
      const Info = '';
      const description = '';
      const jestinformation = '';

      //test
      const result = extractInfo(Info);
    
      //expect
      expect(result).toEqual(expected);
   });
  
     test('client_name', () => {
      //inputs
      const Info = '';
      const description = '';
      const jestinformation = '';

      //test
      const result = extractInfo(Info);
    
      //expect
      expect(result).toEqual(expected);
   });
});
